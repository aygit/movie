pipeline {
   agent any
   
   

  stages {
   stage('checkout') {
     steps {
       checkout scm
     }

  }


  //publish to npm repo
  stage('publish') {
     steps {
      sh 'source ~/.bash_profile'
      sh 'npm publish'
     }

 }
     
 } 
}

